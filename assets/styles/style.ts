import {StyleSheet} from 'react-native';

export const globalStyle = StyleSheet.create({
  text: {
    color: '#fff',
    fontWeight: 'bold',
    fontSize: 25,
    letterSpacing: 1,
    textAlign: 'center',
  },
});
